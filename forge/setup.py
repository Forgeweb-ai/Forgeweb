from setuptools import setup, find_packages

setup(
    name         = "forge",
    version      = "0.1.0",
    description  = "Mobile-first AI codebase generator — Phi-3.5 Mini fine-tuned",
    packages     = find_packages(),
    python_requires = ">=3.11",
)
